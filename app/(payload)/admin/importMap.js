/**
 * Import map généré par Payload — fichier auto-géré via `pnpm cms:generate`.
 *
 * Tant qu'on n'a pas exécuté la génération (qui nécessite la DB pour
 * inspecter les collections), on exporte un objet vide. Payload utilise
 * uniquement les imports custom des éditeurs / fields ; collections
 * standards fonctionnent sans entrée ici.
 */
export const importMap = {};
