# dependencies
node_modules
.pnpm-store

# next.js
.next
out
next-env.d.ts

# production
build
dist

# misc
.DS_Store
*.pem
Thumbs.db
# fichiers temporaires Office (verrous ~$Doc.docx)
~$*

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# turbo
.turbo

# IDE
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# testing
coverage
.nyc_output
playwright-report
test-results
playwright/.cache

# logs
logs
*.log

# OS
.history
*.stackdump

# Claude Code (local settings only)
.claude/settings.local.json
.claude/cache/
.claude/projects/

# Payload CMS (générés au runtime — pas dans le repo)
payload-types.ts
schema.graphql
.payload/
media/

# Helm — sous-charts vendored (re-fetchés par `helm dependency build`).
# Chart.lock EST commité (pin reproductible), pas les archives.
deploy/helm/*/charts/

# Credentials dev — JAMAIS commit (OWASP §10 A02 + A07).
# Convention : tout fichier `.local.md` est ignoré par git.
*.local.md
.dev-credentials.md

# Captures/docs Office pouvant contenir des secrets (ex. tokens ZeptoMail)
docs/*.docx
