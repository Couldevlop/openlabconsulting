node_modules
.next
out
dist
build
coverage
playwright-report
test-results
pnpm-lock.yaml
package-lock.json
yarn.lock
.git
*.min.js
*.min.css
public

# Helm templates contiennent des directives {{ }} qui ne sont pas du
# YAML valide pour prettier — on les exclut.
deploy/helm/**/templates/
deploy/helm/**/charts/

# Migrations Payload : code SQL auto-généré (drizzle) — non formaté.
migrations/

# importMap admin auto-généré par Payload (réécrit au boot du dev server).
app/(payload)/admin/importMap.js
